import { Dimensions } from 'react-native'

export const STATUS_BAR_HEIGHT = 0
export const W_H_PADDING = 24

export const BOTTOM_SHEET_RADIUS = 16

export const W_WIDTH = Dimensions.get('window').width
export const W_HIGHT = Dimensions.get('window').height
export const LONG_SIDE = Math.max(W_WIDTH, W_HIGHT)
